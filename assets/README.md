# Assets

- `icon.png` — the template's own generic icon. It does **not** reproduce the EdgeEver logo or any EdgeEver brand
  identifier. Referenced by the published template via a raw GitHub URL to a tagged commit (see
  `RAILWAY_TEMPLATE.md`).
